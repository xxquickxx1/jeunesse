<div class="row course" style="padding:40px;">

<%= form_for([@course, @courseitem]) do |f| %>
  <% if @courseitem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@courseitem.errors.count, "error") %> prohibited this courseitem from being saved:</h2>

      <ul>
      <% @courseitem.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :intro %><br>
    <%= f.text_area :intro %>
  </div>

  <div class="field">
      <%= f.label :description %>
      <%= f.text_area :description, class: 'wysihtml5' %>

  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_field :image %>
  </div>

  <div class="field">
      <%= f.label :time %>
      <%= f.text_field :time %>
  </div>
  <div class="field">
    <%= f.label :publish %><br>
    <%= f.check_box :publish %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

</div>
